: comment Compiled using Eoghan's flap to clac compiler https://github.com/Eoghanmc22/flap-compiler ;

: comment flap source code:
    const int PROT_EXEC = 4;
    const int PROT_READ = 1;
    const int PROT_WRITE = 2;
    const int PROT_NONE = 0;
    
    const int MAP_PRIVATE = 2;
    const int MAP_ANON = 32;
    
    const int STDIN = 0;
    const int STDOUT = 1;
    const int STDERR = 2;
    
    const int NULL = 0;
    
    const int PROT_EXEC = 4;
    const int PROT_READ = 1;
    const int PROT_WRITE = 2;
    const int PROT_NONE = 0;
    
    const int MAP_PRIVATE = 2;
    const int MAP_ANON = 32;
    
    const int NULL = 0;
    
    int handle_error(int res) {
      if (res == -1) {
        print(67676767);
        quit();
      }
    
      res
    }
    
    int write(int fd, char *buf, int count) {
      handle_error(syscall(1, fd, (int)buf, count, 0, 0, 0))
    }
    
    char *mmap(char *addr, int length, int prot, int flags, int fd, int pgoffset) {
      (char *)handle_error(
          syscall(0x09, (int)addr, length, prot, flags, fd, pgoffset))
    }
    
    char *malloc(int size) {
      const int prot = PROT_READ + PROT_WRITE;
      const int flags = MAP_PRIVATE + MAP_ANON;
    
      mmap((char *)NULL, size, prot, flags, -1, 0)
    }
    
    int strlen(char *str) {
      if (*str == '\0') {
        0
      } else {
        1 + strlen(str + 1)
      }
    }
    
    typedef struct {
      int[2] x;
    } test;
    
    test it = struct {
      x = [ 1, 5 ];
    };
    
    test *heap = (test *)malloc(sizeof(it));
    *heap = it;
    
    print(heap->x[0]);
    print(heap->x[1]);
;

: comment Check that the clac interperter is using the correct native width ;
64 width_native - if -100 print quit

: comment Start Definitions ;
: func-on_true-3 67676767 print quit drop ;
: func-handle_error-0 1 pick -1 - if 0 1 skip 1 2 pick 2 pick if func-on_true-3 1 skip drop 2 pick 3 2 drop_range ;
: func-write-9 1 4 pick 4 pick 4 pick 0 0 0 syscall 1 pick 5 4 drop_range func-handle_error-0 ;
: func-mmap-14 9 7 pick 7 pick 7 pick 7 pick 7 pick 7 pick syscall 1 pick 8 7 drop_range func-handle_error-0 ;
: func-malloc-22 0 2 pick 3 34 -1 0 7 1 drop_range func-mmap-14 ;
: func-on_true-28 0 swap drop ;
: func-on_false-30 1 pick 1 + 1 pick func-strlen-24 1 2 pick + 1 pick 5 4 drop_range ;
: func-strlen-24 1 pick read8 1 pick 0 - if 0 1 skip 1 3 pick 2 pick 5 3 drop_range if func-on_true-28 1 skip func-on_false-30 ;

: comment Start Main ;
2 func-malloc-22 1 pick 1 write_native 1 pick 8 + 1 pick 5 write_native 2 pick read_native 3 pick 8 + read_native 2 pick print 4 pick read_native 5 pick 8 + read_native 1 pick print