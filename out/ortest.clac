: comment Compiled using Eoghan's flap to clac compiler https://github.com/Eoghanmc22/flap-compiler ;

: comment flap source code:
    const int PROT_EXEC = 4;
    const int PROT_READ = 1;
    const int PROT_WRITE = 2;
    const int PROT_NONE = 0;
    
    const int MAP_PRIVATE = 2;
    const int MAP_ANON = 32;
    
    char *mmap(char *addr, int length, int prot, int flags, int fd, int pgoffset) {
      (char *)(syscall(0x09, (int)addr, length, prot, flags, fd, pgoffset))
    }
    
    int munmap(char* addr, int length) {
      (syscall(0x0b, (int)addr, length, 0, 0, 0, 0))
    }
      
    char *malloc(int size) {
      const int prot = PROT_READ + PROT_WRITE;
      const int flags = MAP_PRIVATE + MAP_ANON;
    
      mmap((char *)0, size, prot, flags, -1, 0)
    }
    
    
    void main() {
      char* buf = malloc(4096);
    
      *buf = 'q';
    
      char buf2 = 'q';
    
      print((int)(*buf));
      // print((int)((*buf) == ' ')); // 0
      // print((int)((*buf) == '\n')); // 0
    
      print((int)(((*buf) == ' ') || ((*buf) == '\n')));
    
      print((int)((buf2 == ' ') || (buf2 == '\n')));
    
      // if ((bool)(((*buf) == ' ') || ((*buf) == '\n'))) {
      //   print(1); // 1
      // } else {
      //   print(0);
      // }
    }
    
    main()
;

: comment Check that the clac interperter is using the correct native width ;
64 width_native - if -100 print quit

: comment Start Definitions ;
: func-mmap-0 9 7 pick 7 pick 7 pick 7 pick 7 pick 7 pick 13 6 drop_range syscall ;
: func-munmap-7 11 3 pick 3 pick 0 0 0 0 9 2 drop_range syscall ;
: func-malloc-10 0 2 pick 3 34 -1 0 7 1 drop_range func-mmap-0 ;
: func-main-12 4096 func-malloc-10 1 pick 113 write8 1 pick read8 1 pick print 2 pick read8 1 pick 32 - if 0 1 skip 1 4 pick read8 1 pick 10 - if 0 1 skip 1 3 pick 2 pick if 0 1 skip 1 if 0 1 skip 1 * 1 swap - 1 pick print 113 32 - if 0 1 skip 1 113 10 - if 0 1 skip 1 2 pick 2 pick if 0 1 skip 1 if 0 1 skip 1 * 1 swap - 1 pick print 10 10 drop_range ;

: comment Start Main ;
func-main-12