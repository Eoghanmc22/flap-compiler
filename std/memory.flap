void memcpy_int(int *src, int *dest, int len) {
  if (len > 0) {
    *dest = *src;
    memcpy_int(src + 1, dest + 1, len - 1);
  }
}

void memcpy_char(char *src, char *dest, int len) {
  if (len > 0) {
    *dest = *src;
    memcpy_char(src + 1, dest + 1, len - 1);
  }
}
