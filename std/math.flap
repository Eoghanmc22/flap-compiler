#include "memory.flap"

int abs(int x) {
  if (x >= 0) {
    x
  } else {
    -x
  }
}

// rounds x to the next multiple of 4
int roundup_4(int x){(x + 3) & -4}

int roundup_page_size(int x) {
  (x + PAGE_SIZE - 1) & -PAGE_SIZE
}
