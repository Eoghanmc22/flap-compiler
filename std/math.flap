#include "memory.flap"

int abs(int x) {
  if (x >= 0) {
    x
  } else {
    -x
  }
}

// rounds x to the next multiple of 4
int roundup_4(int x){(x + 3) & -4}

// rounds x to the next multiple of 8
int roundup_8(int x){(x + 7) & -8}

