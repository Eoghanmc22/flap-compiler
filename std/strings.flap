#include "io.flap"

int strlen(char *str) {
  if (*str == '\0') {
    0
  } else {
    1 + strlen(str + 1)
  }
}

void print_str(char *str) {
  int len = strlen(str);

  write_all(STDOUT, str, len);
  new_line();
}
