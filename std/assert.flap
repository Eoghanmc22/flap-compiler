#include "malloc.flap"

void assert(bool x) {
  if (!x) {
    char[20] msg = "Assertion failure!\n\0";

    char *tmp = (char *)malloc(sizeof(msg));
    *tmp = msg;

    write_all(STDOUT, tmp, strlen(tmp));

    free((void *)tmp);

    quit()
  }
}
