#include "malloc.flap"

void assert(bool x, int line) {
  if (!x) {
    auto msg = "Assertion failure! On line: ";

    char *tmp = (char *)malloc(sizeof(msg) / 8);
    *tmp = msg;

    write_all(STDOUT, tmp, sizeof(msg) / 8);
    print(line);

    free((void *)tmp);

    quit()
  }
}
