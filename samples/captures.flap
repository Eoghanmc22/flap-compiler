int bb(int x) {x}
int x = bb(5);

void do_thing() {
  print(x);
  if (x != 0) {
    print(x);
  }
}

do_thing();

int test(int x) {
  int inner(int y) {
    print(x);

    if (true) {
      inner(y)
    }
  }

  inner(6)
}

test(5);
