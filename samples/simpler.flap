// int i = 14 + 12;
// print(i);
// return 12;

// int test() {
  // int i = 42;
  // int j = i + 37;
  // return j ** 5;
// }
// test();

int sqrt(int num) {
  int sqrt_inner(int i) {
    if (i*i > num) {
      return i-1;
    }
    return sqrt_inner(i+1);
  }

  return sqrt_inner(0);
}
