// static can only be set to literals

static int test = 5;

int sqrt(int num) {
  int sqrt_inner(int i) {
    if (i*i > num) {
      return i-1;
    }
    return sqrt_inner(i+1);
  }

  return sqrt_inner(0);
}

int i = 14;
print(i);
print(sqrt(i));
